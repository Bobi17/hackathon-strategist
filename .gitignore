# deps
node_modules/
.pnpm-store/
.venv/

# build & test output
dist/
coverage/
# integration-test scratch — project-local so tests never touch the OS temp dir
.test-tmp/

# the Strategist's generated artifacts — never commit a run
output/

# env & secrets
.env
.env.local
.env.*.local

# agent scratch
.agent-logs/
# understand-anything transient scratch — the knowledge graph itself is committed
.ua/tmp/

# os / editor
.DS_Store
*.log
.vscode/
.idea/
